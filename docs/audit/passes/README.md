# Audit pass reports

The `hardening-audit` workflow writes one `PASS-<n>.md` here per run: the coverage
it exercised, the verified findings (with repros), the mutation-canary escapes, and
the convergence/residual-risk summary. These are the durable evidence trail — a
verdict you can re-check, not one you have to re-trust.
