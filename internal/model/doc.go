// Package model defines LazyPlanner's core domain types (Event, Todo,
// Calendar) and recurrence expansion. It is pure data and logic with no I/O,
// so it is fully unit-testable and imports neither the filesystem nor the
// network.
package model
